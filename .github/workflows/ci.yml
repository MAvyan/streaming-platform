name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    name: ${{ matrix.project }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project: [streaming, admin, backend]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ matrix.project }}/package-lock.json

      # npm install (et non npm ci) pour contourner le bug npm des dépendances
      # optionnelles natives (npm/cli#4828) qui n'installe pas les binaires
      # de plateforme (oxlint, rollup, tailwind-oxide) sur le runner glibc.
      - name: Install dependencies
        working-directory: ${{ matrix.project }}
        run: npm install --no-audit --no-fund

      - name: Generate Prisma client
        if: matrix.project == 'backend'
        working-directory: ${{ matrix.project }}
        run: npx prisma generate

      - name: Lint
        working-directory: ${{ matrix.project }}
        run: npm run lint

      - name: Build
        working-directory: ${{ matrix.project }}
        run: npm run build

      - name: Test + coverage
        working-directory: ${{ matrix.project }}
        run: npm run test:coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.project }}
          path: ${{ matrix.project }}/coverage
          if-no-files-found: ignore
